export const InlineIcon = ({ name, alt }) => (
  <img
    src={`/images/icons/${name}.svg`}
    alt={alt || name}
    style={{
      display: "inline-block",
      verticalAlign: "middle",
      width: "24px",
      height: "24px",
      lineHeight: "1em",
    }}
  />
);

;
